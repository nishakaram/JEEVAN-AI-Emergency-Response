# Optional: only needed for real AI classification (Phase 4).
# If left empty, JEEVAN automatically uses the deterministic mock classifier
# — the app works correctly either way, this just controls which one runs.
ANTHROPIC_API_KEY=

DATABASE_URL=sqlite:///../database/jeevan.db
DEMO_MODE=true
